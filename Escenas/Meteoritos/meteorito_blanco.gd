extends CharacterBody2D

@export var player : PackedScene
const SPEED = 100

func _physics_process(delta: float) -> void:
	pass
	#var direction = (nave_player.global_position - global_position).normalized()
	
